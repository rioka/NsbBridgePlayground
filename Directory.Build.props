<Project>

  <PropertyGroup>
    <TargetFramework>net7.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- NsbBridgePlayground.Bridge requires NserviceBus v8 -->
  <ItemGroup Condition="'$(ProjectName)' != 'NsbBridgePlayground.Bridge' AND '$(ProjectName)' != 'NsbBridgePlayground.Common'">
    <!-- explicit reference to NServiceBus not to get some lower version -->
    <PackageReference Include="NServiceBus" />
    <PackageReference Include="NServiceBus.Transport.SqlServer" />
  </ItemGroup>

</Project>